version: 1

bootstrap:
  - make bootstrap

validate:
  - make validate

run:
  - make run

down:
  - make down

health:
  - make health

provider:
  inspect: make provider-inspect
  audit: make provider-audit
  inspect_timingdata: make provider-inspect TOPIC=TimingData
  export: make provider-export

database:
  backup: make backup
  restore_example: make restore BACKUP_FILE=./.data/backups/<backup-file>.sql.gz
  sql: make sql

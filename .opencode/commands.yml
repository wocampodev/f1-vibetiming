version: 1

bootstrap:
  - make bootstrap

validate:
  - make validate

run:
  - make run

down:
  - make down

health:
  - make health

database:
  backup: make backup
  restore_example: make restore BACKUP_FILE=./.data/backups/<backup-file>.sql.gz
  sql: make sql
